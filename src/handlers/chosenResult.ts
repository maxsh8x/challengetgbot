// Inline chosen result handler — kept as stub since the bot now operates in group mode
import { Context } from 'telegraf';

export async function handleChosenInlineResult(_ctx: Context): Promise<void> {
  // No-op
}
